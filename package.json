{
  "name": "nouveau-dossier-workspace",
  "private": true,
  "description": "Point d'entrée npm — app Sika FLOW (MoMoParse) dans sikaflow-momoparse.",
  "scripts": {
    "dev": "npm run dev --prefix ./sikaflow-momoparse",
    "build": "npm run build --prefix ./sikaflow-momoparse",
    "start": "npm run start --prefix ./sikaflow-momoparse",
    "lint": "npm run lint --prefix ./sikaflow-momoparse",
    "install:app": "npm install --prefix ./sikaflow-momoparse"
  },
  "dependencies": {
    "@supabase/ssr": "^0.9.0",
    "next": "^16.2.1",
    "react": "^19.2.4"
  }
}
